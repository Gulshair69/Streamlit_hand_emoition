streamlit>=1.36.0
# TensorFlow is required for model loading, but Streamlit Community Cloud may
# run newer Python versions where TensorFlow wheels are not available.
#
# We only install TensorFlow when Python < 3.13. If Streamlit uses Python >= 3.13,
# the app will start but show an error telling you to switch Streamlit's Python
# runtime to <= 3.12.
tensorflow==2.16.1; python_version<'3.13' and (platform_machine=='x86_64' or platform_machine=='amd64')
tensorflow-aarch64==2.16.1; python_version<'3.13' and (platform_machine=='aarch64' or platform_machine=='arm64')

# OpenCV is optional (image mode works without it). Video mode will
# show an error if OpenCV isn't installed.

# Keep NumPy below 2.0 to avoid incompatibilities with TF 2.15/2.16.
numpy<2.0

pillow>=9.5.0

# MediaPipe powers multi-face and multi-hand detection.
# If Streamlit runs Python 3.13, skip MediaPipe to avoid install failures.
mediapipe>=0.10.0; python_version<'3.13'
