streamlit>=1.36.0
# Pin TensorFlow to a version that has wheels for the Streamlit Python runtime.
# Streamlit currently uses CPython 3.10/3.11, but we keep a fallback for 3.12.
tensorflow==2.15.1; python_version<'3.12'
tensorflow==2.16.1; python_version>='3.12'

# Use headless OpenCV to avoid GUI/system-library dependency issues on Linux.
opencv-python-headless>=4.8.0.74

# Keep NumPy below 2.0 to avoid incompatibilities with TF 2.15/2.16.
numpy<2.0

pillow>=9.5.0
